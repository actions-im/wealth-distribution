The observation is an SCF family. Monetary values are 2022 dollars, and the SCF family weight WGT produces weighted national totals and family counts. Conventional net worth and all modeled future resources are ranked independently, so the same rank interval does not contain a fixed set of families across the two measures. Age-slicing panels apply the same rule after restricting the data to the respondent-age bucket.
